enum UserRole {
    ADMIN
    CUSTOMER
}

enum Transmission {
    MANUAL
    AUTOMATIC
    SEMI_AUTOMATIC
    NONE
}

enum Fuel {
    PETROL
    OCTANE
    DIESEL
    ELECTRIC
    HYBRID
    CNG
}

enum VehicleStatus {
    AVAILABLE
    RENTED
    MAINTENANCE
    RETIRED
}

enum Unit {
    LITRE
    KWH
    CUBIC_METRE
}
