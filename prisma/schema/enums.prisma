enum UserRole {
    ADMIN
    CUSTOMER
}

enum Transmission {
    MANUAL
    AUTOMATIC
    SEMI_AUTOMATIC
    NONE
}

enum Fuel {
    PETROL
    OCTANE
    DIESEL
    ELECTRIC
    HYBRID
    CNG
}

enum VehicleStatus {
    AVAILABLE
    BOOKED
    RENTED
    MAINTENANCE
    RETIRED
}

enum Unit {
    LITRE
    KWH
    CUBIC_METRE
}

enum PaymentType {
    ADVANCE
    FINAL
    FULL
    REFUND
}

enum PaymentMethod {
    CASH
    STRIPE
    SSLCOMMERZ
    BKASH
    NOGOD
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    REFUNDED
}

enum BookingStatus {
    PENDING
    ADVANCE_PAID
    PICKED_UP
    RETURNED
    COMPLETED
    CANCELLED
}

enum Gender {
    MALE
    FEMALE
}